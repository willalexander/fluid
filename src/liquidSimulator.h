void initialise(float, float, float, float, float, std::vector<rect> *, std::vector<fluidSource> *);		// This is the start point and sets up the fundamental settings for the simulation
void simulate();																							// Runs the simuation																					
void cleanup();																								// Frees dynamically allocated objects
